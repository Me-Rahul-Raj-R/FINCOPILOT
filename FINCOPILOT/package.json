{
  "name": "fincopilot",
  "version": "2.1.0",
  "description": "FinCopilot — AI Risk, Assistance & Payments Ledger for Digital Banking",
  "scripts": {
    "install:all":  "cd backend && npm install && cd ../frontend && npm install",
    "build":        "cd frontend && npm install && npm run build && mkdir -p ../backend/public && cp -r dist ../backend/public/dist",
    "start":        "cd backend && node server.js",
    "dev:backend":  "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "seed:admin":   "cd backend && npm run seed:admin"
  },
  "engines": { "node": ">=18.0.0" }
}
